Movie Pipeline (SQL + ML in One File)
This project builds a tiny end-to-end data pipeline:

Creates a movie dataset (budget, marketing, revenue)
Stores it in SQLite using SQL
Runs SQL query to filter data
Trains a Linear Regression model to predict revenue
Plots Budget vs Revenue using Matplotlib
Runs a Bash command from Python
Prints a sample Dockerfile and cloud upload command
